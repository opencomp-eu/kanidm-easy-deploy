---
services:
  kanidm:
    networks:
      - kanidm-net
      - easydeploy-net

networks:
  easydeploy-net:
    external: true
    name: easydeploy-net
  kanidm-net:
    external: true
    name: kanidm-net
